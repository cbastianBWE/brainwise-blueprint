In `src/components/AppLayout.tsx`, replace only the branding conditional inside `<header>` so that the non-default branch renders both the org logo (when present) and the org name (when present) side by side, instead of choosing one or the other. The default branch (BrainWise icon + wordmark) is unchanged. No other code in the file is touched.