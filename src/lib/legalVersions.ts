// Single source of truth for the version strings recorded at signup.
// Bump these when the corresponding legal document is materially updated.
// Must match the version label shown in the document content
// (see src/content/legal/termsContent.ts, privacyContent.ts).

export const TOS_VERSION = "v1";
export const PRIVACY_VERSION = "v1";
